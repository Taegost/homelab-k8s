# PersistentVolumeClaim — prowlarr-backups
#
# NFS-backed backup storage for Prowlarr, dynamically provisioned by the
# nfs-backups StorageClass.
#
# On Unraid, data is stored at:
#   /mnt/user/Backups/arr-stack/prowlarr-backups/
#
# reclaimPolicy is Retain (set on the StorageClass) — deleting this PVC
# does NOT delete the backup data on Unraid.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-backups
  namespace: arr-stack
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-backups
  resources:
    requests:
      storage: 10Gi
