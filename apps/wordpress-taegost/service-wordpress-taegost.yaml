# Service — wordpress-taegost
#
# Exposes the WordPress deployment internally on port 80.
apiVersion: v1
kind: Service
metadata:
  name: wordpress-taegost
  namespace: wordpress-taegost
  labels:
    app.kubernetes.io/name: wordpress-taegost
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  selector:
    app.kubernetes.io/name: wordpress-taegost
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
