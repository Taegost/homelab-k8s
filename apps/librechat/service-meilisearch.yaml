apiVersion: v1
kind: Service
metadata:
  name: meilisearch
  namespace: librechat
  labels:
    app: meilisearch
    app.kubernetes.io/part-of: librechat
spec:
  selector:
    app: meilisearch
  ports:
    - name: http
      port: 7700
      targetPort: 7700
  type: ClusterIP
