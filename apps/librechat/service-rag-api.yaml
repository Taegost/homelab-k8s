apiVersion: v1
kind: Service
metadata:
  name: rag-api
  namespace: librechat
  labels:
    app: rag-api
    app.kubernetes.io/part-of: librechat
spec:
  selector:
    app: rag-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
