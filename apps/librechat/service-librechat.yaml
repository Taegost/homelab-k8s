apiVersion: v1
kind: Service
metadata:
  name: librechat
  namespace: librechat
  labels:
    app: librechat
    app.kubernetes.io/part-of: librechat
spec:
  selector:
    app: librechat
  ports:
    - name: http
      port: 3080
      targetPort: 3080
  type: ClusterIP
