apiVersion: v1
kind: Service
metadata:
  name: dockge
  namespace: traefik
spec:
  ports:
    - port: 5001
      protocol: TCP
      targetPort: 5001
---
apiVersion: v1
kind: Endpoints
metadata:
  name: dockge
  namespace: traefik
subsets:
  - addresses:
      - ip: 192.168.5.12
    ports:
      - port: 5001
        protocol: TCP
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dockge
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`dockge.home.diceninjagaming.com`)
      kind: Rule
      middlewares:
        - name: default-whitelist
          namespace: traefik
      services:
        - name: dockge
          port: 5001
  tls:
    secretName: wildcard-home-diceninjagaming-com-tls
