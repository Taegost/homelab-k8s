apiVersion: v1
kind: Service
metadata:
  name: uptimekuma
  namespace: traefik
spec:
  ports:
    - port: 3001
      protocol: TCP
      targetPort: 3001
---
apiVersion: v1
kind: Endpoints
metadata:
  name: uptimekuma
  namespace: traefik
subsets:
  - addresses:
      - ip: 192.168.5.12
    ports:
      - port: 3001
        protocol: TCP
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: uptimekuma
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`uptimekuma.home.diceninjagaming.com`)
      kind: Rule
      middlewares:
        - name: default-whitelist
          namespace: traefik
      services:
        - name: uptimekuma
          port: 3001
  tls:
    secretName: wildcard-home-diceninjagaming-com-tls
