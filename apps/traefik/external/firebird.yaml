apiVersion: v1
kind: Service
metadata:
  name: firebird
  namespace: traefik
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
---
apiVersion: v1
kind: Endpoints
metadata:
  name: firebird
  namespace: traefik
subsets:
  - addresses:
      - ip: 192.168.5.9
    ports:
      - port: 80
        protocol: TCP
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: firebird
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`firebird.home.diceninjagaming.com`)
      kind: Rule
      middlewares:
        - name: default-whitelist
          namespace: traefik
      services:
        - name: firebird
          port: 80
  tls:
    secretName: wildcard-home-diceninjagaming-com-tls
