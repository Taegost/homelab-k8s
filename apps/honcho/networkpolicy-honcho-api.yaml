# NetworkPolicy -- honcho-api
#
# Restricts ingress to the API server to only Traefik (for the IngressRoute)
# and hermes-agent (for cluster-internal memory calls). All other ingress is
# denied by the implicit default-deny when policyTypes includes Ingress.
#
# Egress is explicitly allowed to: DNS, Postgres (direct connection bypassing
# PgBouncer), Valkey (cache), and LiteLLM (via external HTTPS domain). All
# other egress is denied by the implicit default-deny when policyTypes includes
# Egress.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: honcho-api
  namespace: honcho
spec:
  podSelector:
    matchLabels:
      app: honcho-api
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # --- Traefik ingress (IngressRoute -> honcho-api:8000) ---
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: traefik
          podSelector:
            matchLabels:
              app.kubernetes.io/name: traefik
      ports:
        - protocol: TCP
          port: 8000
    # --- hermes-agent ingress (cluster-internal memory API calls) ---
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: hermes-agent
      ports:
        - protocol: TCP
          port: 8000
  egress:
    # --- DNS (kube-system CoreDNS) ---
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # --- PostgreSQL (direct connection, bypasses PgBouncer) ---
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: postgres
      ports:
        - protocol: TCP
          port: 5432
    # --- Valkey cache (session caching) ---
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: honcho
          podSelector:
            matchLabels:
              app: honcho-valkey
      ports:
        - protocol: TCP
          port: 6379
    # --- LiteLLM proxy (LLM API calls via Traefik → LiteLLM) ---
    # litellm.diceninjagaming.com resolves to the Traefik MetalLB IP
    # (192.168.5.202), but pods can't hairpin back to a LoadBalancer IP
    # on the same node (MetalLB L2 limitation). Instead, allow egress to
    # the traefik namespace — kube-proxy routes the traffic internally
    # via ClusterIP, bypassing the MetalLB hairpin entirely.
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: traefik
      ports:
        - protocol: TCP
          port: 443
